# Environment files
.env
.env.app
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# Dependencies
node_modules/
.venv/

# Build outputs
dist/
.next/
out/
site/

# Next.js auto-generated (regenerated on every build/dev — would otherwise
# trip the continuous-agent's git_status_clean verifier with 1-line diffs)
next-env.d.ts

# IDE
.vscode/settings.json
.idea/

# Temporary files
*.tmp
*.temp

# Test artifacts
test-results/
playwright-report/
.playwright-mcp/
.playwright-cli/
screenshots/
snapshots/

# Demo video outputs (large binaries)
*.webm
*.mp4

# Local-only scratch (never committed, never pushed)
local-only/

# Shared worktree infrastructure (inherited, not project-specific)
.claude/
CLAUDE.md
